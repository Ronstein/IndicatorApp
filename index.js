/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import { IndicatorApp } from './src/IndicatorApp';

AppRegistry.registerComponent(appName, () => IndicatorApp);
